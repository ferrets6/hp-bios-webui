services:
  bios-webui:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: hp-bios-webui:local
    container_name: hp-bios-webui
    restart: unless-stopped
    ports:
      - "${WEBUI_PORT:-8088}:8000"
